export interface Account {
    accountId: string,
    customerId: string,
    accountType: string,
    balance: number
}

export interface newAccount {
    customerId: string,
    accountType: string
}